package com.jdclone.app

import org.junit.Assert.assertEquals
import org.junit.Test

/**
 * Example local unit test, which runs on the JVM (host) — no device required.
 */
class ExampleUnitTest {
    @Test
    fun addition_isCorrect() {
        assertEquals(4, 2 + 2)
    }
}
